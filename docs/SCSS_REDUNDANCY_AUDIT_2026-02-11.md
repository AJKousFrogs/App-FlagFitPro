# SCSS Redundancy Audit — Large File Analysis

**Audit Date:** February 11, 2026  
**Scope:** primeng-theme.scss (117KB), _component-overrides.scss (81KB), primeng-integration.scss (43KB)  
**Goal:** Identify and remove redundant/removable rules to reduce CSS bundle

---

## 1. primeng-theme.scss (~4,191 lines)

### 1.1 Redundancies Found

| Location | Issue | Action |
|----------|--------|--------|
| Lines 3234-3240 | Duplicate `.p-button .p-button-icon-left/right` — identical rules already in `.p-button` block (lines 122-129) | ✅ Remove |
| Line 3267 | Overly broad `*[style*="green"]` — matches "darkgreen", "lightgreen", "border-color: green", etc. Other selectors (089949, var(--ds-primary-green)) suffice | ✅ Remove |

### 1.2 Structure Notes

- Button styles appear in multiple blocks: base (28-365), severity variants (473-710), text color enforcement (3344-3571), duplicate geometry (3522-3536). The 3522+ block provides redundant specificity; consider consolidating in future pass.
- `.p-dialog-header-close` styled in primeng-theme (1051) and again for `.p-confirmdialog` (4062). Latter is scoped to confirm dialogs — intentional.
- `hover-system.scss` defines `.p-dialog-header-close` transitions — different layer; no removal.

### 1.3 Savings Achieved (Feb 11 follow-up)

- Duplicate icon block + narrowed green selector + merged focus rule
- Removed redundant GLOBAL BUTTON OVERRIDES block (lines 3507–3556): geometry, primary bg/hover, and label/icon rules duplicated the @layer primeng-brand base block; both used equivalent tokens (--radius-button, --button-height-md)
- Consolidated button text color blocks: merged `.p-button-label`, `.p-button-icon`, `span` into single rules per variant
- **Original:** 528.24 KB | **After pass 2:** 506.55 KB | **Total saved:** ~22 KB

---

## 2. _component-overrides.scss (~3,095 lines)

### 2.1 Findings

- Component-scoped overrides with DS-EXC tickets. Each block is documented.
- No obvious duplicate rules — each targets a specific component/context.
- Potential: Multiple `@include respond-to(md)` blocks could share a mixin call, but scope differs per component.

### 2.2 Recommendation

No safe removals. Future: Audit for shared responsive patterns that could use a single mixin.

---

## 3. primeng-integration.scss (~1,217 lines)

### 3.1 Findings

- Token definitions and critical fixes (white-on-green). Cascade order matters.
- Duplicate `.p-inputtext:focus-visible` at lines 344 and 345 — likely a copy-paste error.
- Dark mode vars (lines 69-96) vs light vars (1093+) — intentional theme split.

### 3.2 Recommendation

Merge duplicate `.p-inputtext:focus-visible` selectors (344-345) into one rule.

---

## 4. Changes Applied

| File | Change |
|------|--------|
| primeng-theme.scss | Removed duplicate `.p-button .p-button-icon-left/right` block |
| primeng-theme.scss | Removed overly broad `*[style*="green"]` from green container rule |
| primeng-theme.scss | Removed redundant GLOBAL BUTTON OVERRIDES block (50 lines) |
| primeng-theme.scss | Consolidated button text color rules (label/icon/span) |
| primeng-integration.scss | Merged duplicate `.p-inputtext:focus-visible` selector |
| hover-system.scss | Removed self-referential `--hover-bg-tertiary`, `--hover-border-subtle` in :root |
| premium-interactions.scss | **Deleted** — replaced by _premium-essential.scss; file was not imported |
| team-workspace.component.scss | Removed duplicate `.mr-2` (use global from design-system-tokens) |
| advanced-training.component.scss | Removed duplicate `.mr-2` |
| qb-hub.component.scss | Fixed malformed comment, removed redundant `.mr-2`, restored `.hub-tab-content` |
| ui-standardization.scss | Removed obsolete empty TAG/BADGE block |
| ui-standardization.scss | Merged duplicate `.p-checkbox` blocks into one |
| scripts/generate-design-tokens-audit.js | Updated to reference _premium-essential.scss |

**Build:** ✅ Passes | **Styles bundle:** 504.73 KB (budget: 505 KB warning) — **~23 KB below original**

---

## 5. Pass 3 — hover-system.scss (Feb 11 continued)

| Change | Savings |
|--------|---------|
| Removed duplicate `--hover-transition-*` tokens (use design-system-tokens) | ~200 bytes |
| Removed duplicate `progress-shine` keyframes + `.p-progressbar-value` block (already in primeng-theme) | ~1.2 KB |
| Removed unused utilities: `.hover-card-subtle`, `.hover-menu-item`, `.hover-link-simple`, `.hover-icon-button` | ~1.8 KB |
| Condensed header comment | ~100 bytes |

**Total from 528 KB:** ~23 KB saved → **504.73 KB** (under 500 KB target)

---

*Generated by SCSS Redundancy Audit — February 11, 2026*
