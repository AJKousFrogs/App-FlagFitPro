# V2 — Implementation Tracker

This folder tracks the _build_ against the original v2 vision (an audit + full roadmap; that proposal document was never committed to the repo — the phase docs below are the authoritative record. The v3 counterpart, [`../V3_PROPOSAL.md`](../V3_PROPOSAL.md), _is_ committed). Each phase doc below is a living status record — **status reflects what's actually verified in this repo right now** (typecheck/build/tests run, or explicitly not), not what's planned. Update it in the same PR as the code, same rule as the Feature Status Ledger in `SOURCE_OF_TRUTH.md` §4.

Status legend: **LIVE** (built, verified, migrations applied to the live DB) · **BUILT, NOT DEPLOYED** (built + verified, migration not yet applied to the live DB) · **PARTIAL** (some of the phase's scope landed) · **SPEC** (designed, not built) · **PLANNED** (not started).

| Phase                                                         | Doc                                                          | Status                                                                                                                                                                                                                                                                                                    |
| ------------------------------------------------------------- | ------------------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| V2.0 — Tournament Mode (per-game timeline)                    | [`V2.0-tournament-mode.md`](V2.0-tournament-mode.md)         | LIVE (migration applied in V2.4)                                                                                                                                                                                                                                                                          |
| V2.1 — Travel, supplement timing, season plan                 | [`V2.1-plan-travel.md`](V2.1-plan-travel.md)                 | LIVE (travel legs, consolidated onto `athlete_travel_log` in V2.4) + caffeine timing; season plan builder + KB context tags not started                                                                                                                                                                   |
| V2.2 — Breadth & calibration (coach console, session library) | [`V2.2-breadth-calibration.md`](V2.2-breadth-calibration.md) | PARTIAL — QB throw-count logger built; coach console/session breadth/calibration specced only                                                                                                                                                                                                             |
| V2.3 — Bug sweep                                              | [`V2.3-bugfixes.md`](V2.3-bugfixes.md)                       | 1 real bug fixed (timezone bug in the V2.0 gap engine) + repo-wide lint/typecheck/build/test sweep clean                                                                                                                                                                                                  |
| V2.4 — Global climate & multi-tier competition calendar       | [`V2.4-global-tiers.md`](V2.4-global-tiers.md)               | LIVE — competition-tier taper (continental/world/olympic), heat/cold acclimatization guard, arrival-day load cap. Reconciled V2.1's `event_travel` onto a pre-existing live `athlete_travel_log` table; fixed a live security finding (SECURITY DEFINER trigger functions callable by anon/authenticated) |

**Live-DB status (as of V2.4):** every migration through V2.4 has been applied to the live Supabase project (`grfjmnjpzvknmsxrwesx`) via an authenticated Supabase MCP session — `docs/generated/DATA_MODEL.md`/`ENDPOINTS.md`/`live-schema.snapshot.json`/`supabase-types.ts` are regenerated against it. Any _future_ phase (V2.5+) that adds a migration should re-state this caveat if it can't get authenticated DB access in its own session — don't assume it inherits V2.4's access.
