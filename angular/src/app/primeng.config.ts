/**
 * PrimeNG 21 Pass-Through Configuration
 *
 * All PrimeNG component styling is handled via CSS in:
 * - primeng/_token-mapping.scss (--p-* token mappings)
 * - primeng/_brand-overrides.scss (structural overrides)
 * - primeng/_theme-overlays-panels.scss (overlay/panel styling)
 *
 * This PT config is intentionally minimal — CSS handles everything.
 */

export const PRIMENG_PT_CONFIG = {};
