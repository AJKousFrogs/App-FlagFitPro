/**
 * Default notification preferences used as a fallback when none are stored.
 *
 * @module routes/utils/notification-defaults
 * @version 1.0.0
 */

export const NOTIFICATION_PREFERENCES_DEFAULTS = {
  email: true,
  push: true,
  training_reminders: true,
  wellness_reminders: true,
  team_updates: true,
  achievements: true,
};
