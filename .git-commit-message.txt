feat(design): Today page structural rewrite — athletic chrome

Real structural change, not just typography polish. The Today page
chrome now actually looks like the mockups instead of a SaaS dashboard
with athletic numbers sprinkled on top.

Page level (today.component.scss):
- Radial green gradient atmosphere at top of page (was flat grey)
- Bento items get green-tinted borders + green-glow hover shadow
- Removed @extend chain to %bento-surface-card so the chrome
  is page-owned and dramatic, not generic SaaS

Hero header (today-summary-header.component.scss):
- "Today" greeting jumps to clamp(40px, 5vw, 56px) display font
  with -0.04em letter-spacing (was -0.025em)
- Date sub-line becomes UPPERCASE EYEBROW with 0.08em tracking
- "RECOVERY" kicker gets a pulsing green dot with glow shadow,
  letter-spacing 0.16em
- Hero "~68 min" metric goes from clamp(2.5rem, 5vw, 3.5rem)
  to clamp(56px, 8vw, 96px) — nearly 2× the size
- Hero card gets a radial green glow in upper-right corner
- All sublines become uppercase eyebrows with extrabold weight

Verified: npx tsc --noEmit -p tsconfig.app.json passes clean.
