fix(build): resolve Netlify build errors + Today page chrome rewrite

BUILD FIXES (the Netlify build was failing on the previous push):

NG8113 unused imports removed:
- LoginComponent, RegisterComponent, ResetPasswordComponent,
  UpdatePasswordComponent, VerifyEmailComponent — all dropped
  CardShellComponent from both the import statement AND the @Component
  imports array (the v2 auth pages use the inline auth-shell-v2 pattern,
  so the card shell is no longer used in any template)
- CoachDashboardComponent + PlayerDashboardComponent — same fix
- LandingComponent — dropped ButtonComponent (now uses native router links)

NG8004 missing pipe fixed:
- CoachDashboardComponent now imports DecimalPipe (template uses
  `| number: "1.2-2"` for ACWR display)

TS2339 nonexistent property accesses removed in coach-dashboard.html:
- Removed isHome / time / rsvpCount / rsvpTotal from UpcomingGame
  references (the actual UpcomingGame type only has gameId, date,
  opponent, opponentLogo, location, gameType, daysUntil, teamReadiness)
- Pre-game hero now shows: opponent + date + location + gameType
- Live hero shows date + location
- RSVP block removed (no data source)

NG8102 redundant nullish coalescing:
- trainingSessions()[0].time ?? "" → trainingSessions()[0].time
  (TrainingSession.time is a required string)

PAGE CHROME REWRITE (Today page):
- today.component.scss now overrides the generic %bento-surface-card
  with athletic page chrome: radial green gradient at top of page,
  green-tinted bento borders with glow hover shadow, display-font
  bento titles
- today-summary-header: "Today" greeting jumps to clamp(40px, 5vw, 56px),
  hero "~68 min" metric grows from clamp(2.5rem, 5vw, 3.5rem) to
  clamp(56px, 8vw, 96px) (nearly 2x). "RECOVERY" kicker gets a pulsing
  green dot with glow shadow. All sublines become uppercase eyebrows.

Note: sandbox npx tsc passes; full ng build couldn't be verified in
sandbox (Linux esbuild missing) — Netlify build will be the source
of truth.
